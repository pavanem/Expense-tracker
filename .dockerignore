# The frontend image's build context is the repo root (so it can COPY
# docker/nginx/nginx.conf), so this keeps that build fast by excluding
# everything it doesn't need.
backend/target/
backend/logs/
frontend/node_modules/
frontend/dist/
.git/
.gitignore
*.md
docs/
